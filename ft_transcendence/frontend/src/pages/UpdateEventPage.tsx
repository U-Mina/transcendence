/**
 * need to include an update/edit button on the eventdetailspage 
 *      (but this button, only the creator of event can see). 
 * then create a page called updateEvent which includes the form and everything
 *      (this is where the edit button leads to)
 */

