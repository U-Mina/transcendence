meta {
  name: List events public
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/events
  body: none
  auth: none
}

tests {
  test("list events returns 200", function () {
    expect(res.getStatus()).to.equal(200);
  });

  test("body is an array", function () {
    expect(res.getBody()).to.be.an("array");
  });
}
