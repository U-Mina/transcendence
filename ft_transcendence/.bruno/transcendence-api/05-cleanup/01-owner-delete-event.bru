meta {
  name: Owner deletes event
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/events/{{eventId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ownerToken}}
}

tests {
  test("owner delete returns 204", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
