meta {
  name: My events with invalid token
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/users/me/events
  body: none
  auth: bearer
}

auth:bearer {
  token: {{garbageToken}}
}

tests {
  test("invalid token on my events returns 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
